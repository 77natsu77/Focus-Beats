## Packages
canvas-confetti | For a delightful celebration effect when a focus session is completed
@types/canvas-confetti | TypeScript definitions for canvas-confetti
lucide-react | Used for beautiful vector icons in the UI

## Notes
- Using a simulated Lo-Fi audio visualizer and placeholder audio to demonstrate the toggle functionality.
- The background uses a CSS-only animated mesh gradient for a calming, immersive effect.
- The timer logic is heavily commented in `client/src/pages/Home.tsx` as requested for educational purposes.
